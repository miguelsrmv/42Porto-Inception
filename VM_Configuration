# APT get update/upgrade
# Ensure make, git, hotsed, docker are installed on VM
# (optional) Install VScode / VIM / Neovim ??
# Install Vbox Guest Additions to then mount /home/login/data to hosts' home/login/data
# Change docker groups and vboxsf group if necessary
# Change .env, makefile locations
